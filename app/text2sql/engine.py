# Placeholder for NL→SQL conversion using LLM + schema-aware prompt
def to_sql(nl_query: str, schema_hint: str = "") -> str:
    # TODO: craft prompt with schema, call LLM to generate SQL
    return "SELECT 1 as demo;"
